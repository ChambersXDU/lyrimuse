# 13. 网页展示与中继

本章记录外部网页或中继部署与 Lyrimuse App 的边界。当前仓库没有网页服务端或独立中继运行时；Swift App 的歌词获取链路只负责本地缓存和本地展示，不依赖外部网页功能。

如果部署了外部网页，中继应把它视为独立系统：它可以消费外部状态接口，但不参与 `LyricsResolver`、歌词匹配或本地缓存写入。App 的网络歌词请求只属于五个 Swift Provider。
